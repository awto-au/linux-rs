# Phase 0 — pinned environment

Established 2026-07-16.

## Corpus pin

| Item | Value |
|---|---|
| Kernel | **v7.1** (`8cd9520d35a6`, "Linux 7.1") — latest release at pin time (v7.2-rc3 existed, rejected: rc churn is wrong for a pinned corpus) |
| Tree | `./linux/` — local clone of `/mnt/2tb/git_mirror/linux`, `upstream` remote = torvalds/linux |
| Config | x86_64 `defconfig` + `CONFIG_RUST=y` + `CONFIG_SAMPLE_RUST_MINIMAL=y` (via `olddefconfig`) |
| Corpus size | **3066 C translation units** (`compile_commands.json`, generated by `scripts/clang-tools/gen_compile_commands.py`) |

## Toolchain

| Tool | Version | Status |
|---|---|---|
| clang/LLVM | 22.1.8 (Fedora 44) | ✅ |
| ld.lld | 22.1.8 (installed via dnf during setup) | ✅ |
| rustc | 1.97.0 | ✅ `make LLVM=1 rustavailable` passes |
| bindgen | 0.72.1 | ✅ |
| QEMU | 10.2.2 | ✅ |
| coccinelle (spatch) | — | ❌ not installed; needed for Phase 0 pattern-counting eval |
| c2rust | — | ❌ not installed; needed for Phase 0 transpiler eval |

## Build

`make -C linux LLVM=1 -j32` — completed, `arch/x86/boot/bzImage` produced,
`samples/rust/rust_minimal.o` compiled (Rust-for-Linux kbuild integration
confirmed working on this machine).

Rebuild from scratch: `defconfig` → `scripts/config -e RUST -e SAMPLES
-e SAMPLES_RUST -e SAMPLE_RUST_MINIMAL` → `olddefconfig` → build →
regenerate `compile_commands.json`.

## Boot

QEMU smoke test: `qemu-system-x86_64 -accel kvm -m 1G -nographic -no-reboot
-kernel linux/arch/x86/boot/bzImage -append "console=ttyS0 panic=-1"` —
pass criterion is the v7.1 banner on serial and controlled exit at root-mount
panic (no rootfs yet; a proper initramfs + kselftest image is Phase 2 work).
**Result: PASS** (2026-07-16) — `Linux version 7.1.0 … clang 22.1.8, LLD 22.1.8`
on ttyS0, clean QEMU exit at the expected panic, ~1.0s to panic under KVM.
