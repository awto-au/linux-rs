# History

Curated milestone log for linux-rs. Not exhaustive — see `docs/` for the
full set of phase/scoping/research reports as they land, and
[docs/STATUS.md](status/STATUS.md) / [docs/status/dashboard.html](status/dashboard.html)
for live, auto-regenerated numbers (TU count, KUnit results, work-item
queue). This file is the human-curated narrative; those are the live data.

| Date | Milestone |
|---|---|
| 2026-07-16 | Phase 0 complete: kernel v7.1 pinned, x86_64 defconfig+RUST, LLVM=1 build + QEMU boot verified, Rust-for-Linux working, coccinelle/c2rust evaluated ([docs/phase0-evals.md](phase0-evals.md)) |
| 2026-07-16 | Phase 1 v0 census: 85,773 functions fingerprinted in 76 s. Whole functions do **not** collapse (8.4%); external call vocabulary does (top-2000 APIs cover 51% of functions' call surface) ([docs/phase1-census-v0.md](phase1-census-v0.md)) |
| 2026-07-16 | **Phase 1 v1 census — GATE: GO.** 1.44M statement instances → 199 families cover 50%, 15 cover 25% ([docs/phase1-census-v1.md](phase1-census-v1.md)) |
| 2026-07-16 | **Phase 1 v2: tail is cheap composition.** Singleton statements have median **2** novel glue nodes; 91% of their AST is already-common subtrees ([docs/phase1-census-v2-composition.md](phase1-census-v2-composition.md)) |
| 2026-07-16 | Phase 2 re-scoped: minimal **riscv64** boot path; corpus measured at **511 TUs (~16% of lab)**, slim-serial kernel **boots in QEMU** ([docs/phase2-minimal-target.md](phase2-minimal-target.md)) |
| 2026-07-16 | **Correctness review → census v1.1/v2.1.** Macro-internal inflation, brace bias, type erasure fixed. Corrected gate: **26 families = 25%, 713 = 50%** of 1.06M statements — GO stands; tail: median **1** non-root glue node ([docs/review-findings-2026-07-16.md](review-findings-2026-07-16.md)) |
| 2026-07-16 | Reference corpus added: Asterinas, Moss, Kerla, linux-0.11-rs, rCore as target-design evidence ([docs/reference-projects.md](reference-projects.md)) |
| 2026-07-16 | **First translated TU running in the kernel.** `lib/math/gcd.c` → Rust, in-tree on the riscv64 target, all 11 KUnit vectors pass on the booted kernel — including the static-key fallback path a naive translation would have dropped. 5 rules extracted ([docs/phase2-first-translation.md](phase2-first-translation.md)) |
| 2026-07-16 | **Batch 3 (readiness-ranked): rational, bcd, hweight — 9 Rust TUs, all suites green.** The transfer metric works: after 6 TUs, hweight/bcd were 100% in-vocabulary, rational 96.8% (`scripts/readiness.py`, patches/0004) |
| 2026-07-16 | **First tier-2 TU: `lib/sort.c` heapsort in Rust — `ok 11 lib_sort` on the booted kernel.** Sentinel fn-pointers, raw `void*` arithmetic, callback dispatch, `cond_resched` C shim; rules 0012–0014 (patches/0003) |
| 2026-07-16 | **Batch 2: five Rust TUs in the booted kernel, 58/58 KUnit vectors.** lcm/int_log/int_pow/int_sqrt; first Rust→Rust cross-TU call; rules 0006–0010. Benchmark: **faithful Rust ≡ C** at equal opt level; `isqrt` optimised lane 2.1× — but "idiomatic" gcd 2× slower, so per-function measurement gates the lane ([docs/phase2-batch2-and-bench.md](phase2-batch2-and-bench.md)) |
| 2026-07-17 | `awtoau/c2rust` transpiler fork adopted as a second, faster-but-lower-confidence track alongside hand-translation; systematic Clang-AST-consumption bugs found and fixed upstream in the fork (issue tracking in `rulesdb/patterns.db`'s `c2rust_*` tables) |
| 2026-07-18 | **30 hand-translated TUs, 15 KUnit suites green.** Batches 18–30 (find_bit, cmdline, checksum, decompress, earlycpio, hexdump, div64, parser, string, bitmap, kstrtox, bitmap-str) landed; c2rust track adds real `rustc --emit=metadata` compile-checking against the kernel's own `libcore`, not just transpile-clean status ([docs/status/session-report-2026-07-18.md](status/session-report-2026-07-18.md)) |
| 2026-07-18 | **Hybrid boot-path milestone: first Rust code in a live device driver**, not a whole-file `lib/` swap — `8250_port.c`'s `serial8250_compute_lcr()` now calls a Rust implementation under `CONFIG_RUST`, verified boot-clean with the Rust symbol present in `vmlinux` ([docs/hybrid-boot-milestone-2026-07-18.md](hybrid-boot-milestone-2026-07-18.md), scoping: [docs/serial-8250-translation-scoping-2026-07-18.md](serial-8250-translation-scoping-2026-07-18.md)) |
| 2026-07-18 | tmpfs-in-Rust scoped and found **blocked**: the vendored `rust/kernel/fs.rs` has no VFS filesystem-registration abstractions to attach a Rust tmpfs to — a harder gap than any TU landed so far ([docs/tmpfs-rust-scoping-2026-07-18.md](tmpfs-rust-scoping-2026-07-18.md); tracked as [awto-au/linux-rs#4](https://github.com/awto-au/linux-rs/issues/4)) |
| 2026-07-18 | 32 TUs, 16 KUnit suites / 143 vectors green (`iomem_copy`, TU 32); two-track work-item dashboard added ([docs/status/dashboard.html](status/dashboard.html)) |
| 2026-07-18 | Interactive console milestone: minimal initramfs `/init` now drops to a live `sh` prompt on a bounded 15s input wait instead of always powering off — the prerequisite for the hybrid-boot-backwards stream (see `docs/streams.md`), verified genuinely interactive via a real piped command execution |
| 2026-07-18 | Boot-log history system: every `boot_qemu.py` run now archives its full log, appends a row to the tracked `docs/status/boot-history.csv`, and auto-commits+pushes — diffable (`render_boot_log.py --diff`), single-timeline (`--history`), and browsable-directory (`--browse`) views all generated from the same archive |
| 2026-07-18 | Kernel-track work items (8250 remainder, tmpfs-in-Rust) moved from hand-curated DB rows to real GitHub issues ([awto-au/linux-rs#3](https://github.com/awto-au/linux-rs/issues/3), [#4](https://github.com/awto-au/linux-rs/issues/4)) — both tracks now sync from real issue trackers, not memory |
| 2026-07-18 | Closed awtoau/c2rust#12: convert_warn_on emits 'as c_int' cast on an already-bool condition, mismatching kernel::warn_on!'s bool parameter (E0308) |
| 2026-07-18 | Closed awto-au/linux-rs#1: Licensing: make the repo exactly Linux-compatible |
| 2026-07-18 | **8250 RX-trigger helpers wired to Rust, closes awto-au/linux-rs#3** — `fcr_get_rxtrig_bytes()`/`bytes_to_fcr_rxtrig()` now call Rust under `CONFIG_RUST`, completing the full oracle-verified slice from the 8250 scoping doc; `uart_config[]` stays single-sourced in C via slice-passing (design fork resolved: full table port rejected as disproportionate — ~25 entries incl. a `name` field read well beyond these two functions), zero call-site edits needed; verified via fresh oracle re-run, before/after `dev.py check`, byte-diffed boot transcripts, and `nm vmlinux` symbol linkage ([docs/hybrid-boot-milestone-8250-trigger-2026-07-18.md](hybrid-boot-milestone-8250-trigger-2026-07-18.md)) |
| 2026-07-18 | Closed awtoau/c2rust#11: goto-to-labeled-block lowering: break targets '___UNIQUE_ID_label_N that was never declared (block gets a different synthesized name) — 6/228 files, E0426 |
| 2026-07-18 | Closed awto-au/linux-rs#3: 8250/16550 serial driver translation — remaining register-bit helpers (fcr_get_rxtrig_bytes, bytes_to_fcr_rxtrig) |
| 2026-07-18 | linux-riscv/ pushed to a real GitHub fork (awtoau/linux, branch linux-rs/phase2-gcd) — 34 landed TUs were local-only until now, matching the durable-fork pattern already used for awtoau/c2rust |
| 2026-07-18 | rulesdb #6 Phase 2 (a1/a2): deduped dev.py/integrate_tu.py KUnit boot-oracle regex into scripts/kunit_oracle.py, rulesdb/rules/0028-kunit-boot-oracle-gate.toml documents the hard-FAIL gate plus INIT_REACHED WARN clause |
| 2026-07-18 | rulesdb #6 Phase 2 (a3): mechanical EXPORT_SYMBOL->_GPL silent-upgrade WARN check wired into dev.py check + check_c2rust_output_compiles.py; 135/163 checkable #[export] sites deviate; check_c2rust_rule_conformance.py rule 0001 stub replaced with a real checker |
| 2026-07-18 | **35 hand-translated TUs, 16 KUnit suites green.** lib/strncpy_from_user.c -> Rust: word-at-a-time + byte-at-a-time fallback strncpy over a userspace pointer, sharing strnlen_user's unsafe_get_user fault-shim family plus one new byte-sized shim (rust_helper_unsafe_get_user_u8); oracle 2.5 PASS 2500 cases (2 distinct EFAULT triggers both exercised), SPDX PASS 34/34, ORACLE PASS 16 suites, INIT REACHED, nm vmlinux confirms 5 real callers (fs/namei.o, fs/xattr.o, kernel/sys.o, kernel/reboot.o, mm/maccess.o) |
| 2026-07-18 | Closed awto-au/linux-rs#15: Pipeline audit Phase 2 remainder: a4 (c2rust compile-pass-rate regression gating) + a5 (bench_math.py checksum-agreement not gated) |
| 2026-07-18 | **36 hand-translated TUs, 17 KUnit suites green — 8250 Tier B lands, closes awto-au/linux-rs#16 (partial).** `mem_serial_in`/`mem_serial_out` (real `unsafe` MMIO via `bindings::readb`/`writeb`) translated to `drivers/tty/serial/8250/8250_io_rs.rs`; first genuine in-kernel Rust KUnit suite in this project (`rust_8250_mem_serial_io`, 4 cases against a fake `[u8; N]` register backing, not diff-oracle) verifies offset/regshift/truncation/no-crosstalk byte-shuffling. `8250_port.c` untouched (zero diff) — compiled in and unit-tested but not wired to the live `serial_in`/`serial_out` function pointers, per the task's gate. Other 5 iotype variants (`mem16/32/32be`, `io_*`, `hub6_*`, `no_*`) scoped out: zero real coverage on this project's QEMU-virt target (confirmed via `8250_of.c` probe trace). Tier C (startup/shutdown/set_termios/IRQ/console-write) remains untouched, deferred. Incidental fix: `dev.py boot`/`check` didn't resolve `linux-riscv-worktrees/<name>` trees (`TREE.name` stripped the subdir) — 2-line fix ([docs/8250-tier-b-scoping-2026-07-18.md](8250-tier-b-scoping-2026-07-18.md)) |
| 2026-07-18 | Closed awto-au/linux-rs#6: Pipeline audit: generalize ad-hoc verification checks into rulesdb idioms |
| 2026-07-18 | **37 hand-translated TUs, 17 KUnit suites green.** lib/usercopy.c -> Rust: check_zeroed_user() word-at-a-time zero-check over a userspace buffer (new early-exit-shared-label control-flow shape, rule 0020 extended) plus _copy_from_user/_copy_to_user thin FFI wrappers around the real _inline_copy_from_user/_inline_copy_to_user header inlines (two new rust_helper_inline_copy_from_user/_to_user shims) -- confirmed via nm on the prior build's lib/usercopy.o that INLINE_COPY_USER is undefined for this riscv64/MMU .config, so all three C functions are genuinely compiled, none dead code. Oracle 2.5 PASS 2500+50000 cases, SPDX PASS 35/35, ORACLE PASS 16 suites, INIT REACHED, nm vmlinux confirms real callers across fs/open.o, fs/xattr.o, kernel/fork.o, kernel/ptrace.o, kernel/signal.o and more |
| 2026-07-18 | Closed awto-au/linux-rs#7: Consider making missing initramfs reachability a hard gate once it is required |
| 2026-07-18 | **Raw QEMU boot logs now carry real per-line elapsed-time timestamps** (`NNNNN.NNN ` — 5-digit zero-padded whole seconds + milliseconds since the QEMU process started, e.g. `00000.847 ok 1 rust_kernel_str`). `boot_qemu.py` streams QEMU's stdout line-by-line via Popen (was a blocking subprocess.run) so each line gets a real arrival timestamp, not a post-hoc one. All five parsing sites that scan this raw log with column-0-anchored regexes now tolerate the optional prefix via one shared fragment (`kunit_oracle.TS_PREFIX_RE`): kunit_oracle.py's OK_RE/NOT_OK_RE (the shared pass/fail oracle), integrate_tu.py's per-suite anchor, boot_qemu.py's own summary scan, render_boot_log.py's OK_RE/NOTOK_RE/TOTALS_RE (plus classify() now renders the timestamp as its own muted-color column), and report.py's kunit_results()/suite_vectors(). Verified backward-compatible against all 42 archived logs in docs/status/boot-logs/ (40 pre-timestamp, 2 new) with zero parse mismatches; dev.py check before/after both show ORACLE PASS (17 suites), INIT REACHED, SPDX PROVENANCE PASS — identical verdict, format-only change. |
| 2026-07-18 | 8250 Tier C (startup/shutdown/set_termios/IRQ-path/autoconfig, awto-au/linux-rs#25) evaluated and found **blocked**, no code written. Read all four candidate functions plus `autoconfig`; ranked `serial8250_do_startup`/`_do_shutdown` as least-bad of the five but still irreducibly bound to real IRQ-core calls (`request_irq` via `serial_link_irq_chain`, `synchronize_irq`, `free_irq`) and real spinlock guards with no meaningful KUnit fake — unlike Tier B's register-file fake, there's no way to stand in for "the interrupt subsystem accepted this registration" without reimplementing IRQ-core internals inside the test. `serial8250_clear_fifos`/`serial8250_clear_interrupts` (the pure-register-op pieces startup/shutdown call) are already shared/exported across other call sites, so scoping down to just those isn't "the startup/shutdown slice" either. Issue #25 left open with findings posted as a comment ([docs/8250-tier-c-blocker-2026-07-18.md](8250-tier-c-blocker-2026-07-18.md)) |
| 2026-07-18 | **38 hand-translated TUs, 17 KUnit suites green — 8250 Tier C first slice lands, LIVE-WIRED, awto-au/linux-rs#25 (stays open).** `serial8250_do_startup`/`serial8250_do_shutdown` control flow translated to `drivers/tty/serial/8250/8250_startup_rs.rs`; `struct uart_port`/`uart_8250_port` stay opaque `*mut c_void` (no bindgen bindings exist for either), every field access/subsystem call exposed as a narrow `extern "C"` shim in `8250_port.c` (44 functions) — Rust carries control flow only. Wired into the live `.startup`/`.shutdown` uart_ops via a new `CONFIG_RUST_8250_STARTUP` Kconfig option, deliberately independent of `CONFIG_RUST` (which already gates Tier A/B in this driver) so the two paths could be A/B-built without disturbing each other. Explicit, scoped exception to this project's KUnit-gate discipline (Dan, 2026-07-18): these two functions call real `request_irq`/`synchronize_irq`/`free_irq` with no KUnit-fakeable substitute, per `docs/8250-tier-c-blocker-2026-07-18.md`; gated instead on the byte-for-byte side-by-side boot-transcript comparison from the original scoping doc — 3 repeat boots per side (C-path vs. Rust-path), clean from `KTAP version 1` through end of transcript across all 6 logs, zero deviation; pre-KUnit early-boot diffs (line-order jitter, a runtime benchmark value, expected per-binary artifacts) confirmed benign by observing the same jitter within repeated boots of the *same* binary. `dev.py check`: 17/17 KUnit suites, SPDX PROVENANCE PASS 37/37 ([docs/8250-tier-c-startup-shutdown-2026-07-18.md](8250-tier-c-startup-shutdown-2026-07-18.md)) |
| 2026-07-18 | Closed awtoau/c2rust#19: Emit #[export] (not plain #[no_mangle]) for EXPORT_SYMBOL*/EXPORT_SYMBOL_GPL* functions |
| 2026-07-18 | VFS abstraction rebase (upstream RFL PR #1037, awto-au/linux-rs#4) attempted in an isolated worktree and found **not viable right now** — a real `git rebase` in `linux-riscv-worktrees/vfs-rebase-eval` hit substantive conflicts on the first two of 29 commits plus a completely disjoint, partially-obsolete Rust unstable-feature-gate set. Corrects the prior scoping doc's "~1 month old ancestor" finding: that was a misread of GitHub's dynamically-recomputed PR `base.sha`, not the PR branch's actual fork point — the real fork point (`git merge-base`) is `a7135d1`, dated 2023-10-15, **247,018 commits and 2.75 years** behind current HEAD. All 29 PR commits are still dated 2023-10-17/18 (never rebased since); its Rust code targets a rustc nightly from that era, using unstable feature gates (`offset_of`, `return_position_impl_trait_in_trait`) since stabilized-and-removed, which would hard-fail compilation on this project's actual `nightly-2026-07-09` toolchain even after conflicts were resolved. Rebase aborted cleanly, nothing landed in `linux-riscv/` or pushed to `awtoau/linux`; re-closed as blocked with a corrected effort estimate (multi-session rewrite, not a single-session rebase) ([docs/tmpfs-vfs-rebase-2026-07-18.md](tmpfs-vfs-rebase-2026-07-18.md)) |
| 2026-07-18 | Closed awto-au/linux-rs#26: clippy on c2rust raw transpiled output (Tier A #1 from research-pipeline-improvements) |
| 2026-07-18 | **First real combined-image boot with raw c2rust output** (issue #28 proof-of-mechanism): `lib/group_cpus.c`'s c2rust-clean, rule-conformant translation wired into a real kernel behind a new `CONFIG_RUST_C2RUST_BOOT_TEST` gate. "Mechanically clean" turned out not to mean "compiles in this kernel" — three real, corpus-wide gaps found and hand-fixed (disallowed unstable features vs. this kernel's `scripts/Makefile.build` feature allowlist, 26 missing explicit `unsafe{}` wrappers, a userspace-ELF `.init_array` constructor trick the kernel linker discards). All three environments boot clean: the new combined image (17/17 KUnit), the existing hand-translated kernel (17/17), and a pure-C baseline with `CONFIG_RUST` off (9/9, no Rust tests) ([docs/combined-boot-attempt-2026-07-18.md](combined-boot-attempt-2026-07-18.md)) |
| 2026-07-19 | Closed awto-au/linux-rs#29: KBUILD_RUSTFLAGS never gets riscv-march-y's Zacas/Zabha (and other) ISA extensions C code receives |
| 2026-07-19 | Closed awtoau/c2rust#27: refcount.h overflow-detection idiom: signed +/- panics instead of gracefully wrapping (fixed in 1f8c61cf2) |
| 2026-07-19 | Closed awto-au/linux-rs#30: c2rust boot-screening (klist.c): fabricated register-variable static was genuinely live, not dead — plus 2 new build gaps (LIST_POISON const-eval, param/type name collision) |
| 2026-07-19 | Closed awto-au/linux-rs#36: c2rust boot-screening (lib/tests/test_sort.c): literal translation of overflow-reliant C arithmetic panics at runtime under Rust's default overflow checks |
| 2026-07-19 | Closed awto-au/linux-rs#39: refcount.h overflow-detection idiom panics under -Coverflow-checks=y (c2rust-side fix landed) |
